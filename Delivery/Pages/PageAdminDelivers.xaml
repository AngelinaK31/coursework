<Page x:Class="Delivery.Pages.PageAdminDelivers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Delivery.Pages"
      mc:Ignorable="d" 
      
      Title="PageAdminDelivers">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Height="25">
                <TextBox Width="500"/>
                <Button Margin="0">
                    <Image Source="/Delivery;component/Resources/find.png"  />
                </Button>
                <Button Content="Фильтры" Margin="1,0,0,0" Click="btnFiltersClick"/>
            </StackPanel>
            <Grid Visibility="Collapsed" Name="gridFilters" Width="581">
                <Button Height="50"/>
            </Grid>
        </StackPanel>
        

    </Grid>
</Page>
